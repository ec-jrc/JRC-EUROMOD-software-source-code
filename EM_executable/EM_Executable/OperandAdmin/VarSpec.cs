namespace EM_Executable
{
    /// <summary> helper class for variable specification, used if name and index cannot be obtained at the same time </summary>
    internal class VarSpec
    {
        internal string name;       // name of the totals variable
        internal int index;         // index of the totals variable
    }
}
